# This file may be used to create an environment for alignment visualiztion using:
# platform: linux-64
# conda env update --file vis_aln_env.yml --prune

name: minion_vis_aln
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.9.18
  - biopandas
  - biopython
  - ipykernel
  - ipympl
  - ipywidgets
  - jupyterlab
  - matplotlib
  - numpy
  - pandas
  - panel
  - pillow
  - pip
  - pysam
  - samtools

  - pip:
    - igv-notebook